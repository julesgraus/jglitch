export const imageBlockDefaultOptions = {
    blockSizeX: 32,
    blockSizeY: 32,
    minDuration: 100,
    maxDuration: 200,
    intensity: 1,
}

export const imageOffsetLinesDefaultOptions = {
    blockSizeY: 10,
    randomizeBlockSize: true,
    blockCount: 4,
    minDuration: 100,
    maxDuration: 2500,
    minDutyCyclePercentage: 2,
    maxDutyCyclePercentage: 15
}
